/*
 * L1-001 - Hello World（5 分）
 * 
 * 题目描述：
 * 这道超级简单的题目没有任何输入，只需要在一行中输出著名短句"Hello World!"。
 * 
 * 实现原理：
 * 1. 这是一个最简单的程序，不需要任何输入处理
 * 2. 直接调用标准输出函数 println() 输出固定字符串即可
 * 3. main() 函数是程序的入口点，程序从这里开始执行
 */

// 程序入口函数，没有参数，返回类型为 Unit（无返回值）
main() {
    // 使用 println 函数输出字符串到控制台
    // println 会自动在输出末尾添加换行符
    println("Hello World!")
}